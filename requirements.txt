# Core
pyaudio>=0.2.13
numpy>=1.24
pynput>=1.7

# Transcription (local) — at least one required unless using --engine api
faster-whisper>=1.0.0      # recommended: faster, CUDA-accelerated
# openai-whisper>=20231117  # alternative: original OpenAI implementation

# Voice Activity Detection (highly recommended)
silero-vad>=6.0.0
torchaudio>=2.0.0

# Config file support (Python < 3.11 only — Python 3.11+ has tomllib built-in)
# tomli>=2.0.0

# Optional: for --engine api (no local model needed)
# The API engine uses Python's built-in urllib — no extra packages required.
# Tested with: OpenAI, Groq, local whisper.cpp server
